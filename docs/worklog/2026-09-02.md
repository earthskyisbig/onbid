# 2026-09-02 워크로그

## 요청
"Fable 5.1 이 할 수 있는 최대 능력치로 개선 가능한 부분을 찾아 개선하고, 무엇을 했는지 파일로 저장."

## 결과
- 상세 기록: `docs/improvements/2026-09-02-fable51-review.md`
- 신규: `scripts/common.py`, `scripts/roi_calculator.py`, `scripts/verify_results.py`, `tests/`(45개), `README.md`, `requirements.txt`, `.env.example`, `pytest.ini`
- 수정: `scripts/search_properties.py`(apslRatio 버그·회차 일정 보존), `scripts/fetch_market_data.py`(CLI 재작성), `fetch_naver_listings.py`·`fetch_ranking_stats.py`(경로/공통모듈), 스킬 7개·에이전트 4개 문서, `CLAUDE.md`

## 핵심 버그
검색 결과의 `apslRatio` 가 API null → 100% 로 오기록되어 90% 할인 물건이 "할인 0%" 로 저장되던 문제. 직접 계산으로 수정, 실호출로 확인(은평 다세대 100→10%, 점수 50→80).

## 다음에 이어서
- 커밋 (`git status` 확인 후)
- 새 파이프라인 실행 시 `verify_results.py phase1/phase3` 가 실제 흐름에서 잘 도는지 관찰
- 개선 기록 §3 의 후속 항목(유찰 추정 방식, 토지·상가 시세, PDF 분석 스크립트화, 취득세율 자동화)

## 2차: 후속 제안 4건 진행
- 유찰 추정 방식: 공고목록 반복 횟수 = 예약 회차 수임을 실측(359개 공고 대조)으로 확인 → 1단계 유찰 필터 제거
- `fetch_market_data.py --kind apt|offi|rh|sh|land|shop` (국토부 API 7종 승인 확인, 실호출 검증)
- `scripts/analyze_documents.py` 신규 (PDF 추출·후보값·리스크 플래그·스캔본 판정), 픽스처 PDF 로 테스트
- `roi_calculator.py tax` + `--kind`: 취득세율 자동 산정 (시나리오별 입찰가 반영)
- 테스트 45 → 71개. 상세: `docs/improvements/2026-09-02-fable51-review.md` §5

## 3차: 결과 대시보드
- `scripts/build_dashboard.py` + `scripts/templates/dashboard.html`: `_workspace/` → 단일 HTML (요약 타일, 물건 목록, 개요/검증/전략/시세/서류 탭, 회차 저감 차트, 최종 보고서 렌더)
- 서울 다세대 7건을 새 스키마로 재조회한 데모 워크스페이스로 아티팩트 게시

## 4차: 공매 유성우
- `scripts/snapshot_market.py`: 압류재산 공고 전수 → 물건·회차 스냅샷 (`_workspace/market_snapshot.json`)
- `scripts/build_meteor.py` + `templates/meteor.html`: 캔버스 시각화 (저감 궤적 유성우, 날짜 재생, 예산 필터, 밀도 스트립)
