# 2026-07-26 워크로그

## 1. 저장소 설치
- `github.com/earthskyisbig/onbid` 클론 (`C:\Users\m9938\onbid`)

## 2. 온비드 신규 API 6종 조사·구현
- 활용신청 승인된 6개 API의 정확한 오퍼레이션명·파라미터를 실제 호출 + docx 가이드 대조로 검증
  - `OnbidInqRnkClgSrvc/getInqRnkClg` (조회수 순위)
  - `OnbidItrsCltrRnkClgSrvc/getItrsCltrRnkClg` (관심물건 순위)
  - `Onbid50PctDecrCltrSrvc/get50PctDecrCltr` (저감률 순위)
  - `OnbidRlstListSrvc2/getRlstCltrList2` (부동산 물건목록)
  - `OnbidCltrBidRsltDtlSrvc2/getCltrBidRsltDtl2` (물건 입찰결과상세)
  - `OnbidUsgBidStatsSrvc/getKamcoCltrUsgStats`, `getOrgCltrUsgStats` (용도별 입찰통계)
- 신규 스킬 `.claude/skills/onbid-ranking-stats/SKILL.md` + `scripts/fetch_ranking_stats.py` 작성, 6개 커맨드 전부 실데이터로 검증

## 3. 환경설정
- `.env` 생성 (`ONBID_API_KEY`, `MOLIT_API_KEY` — 공공데이터포털 계정당 공통 디코딩키 1개로 양쪽 채움)

## 4. 경기도 아파트 검색·추천 → 중대 버그 발견·수정
- `search_properties.py`가 `/Users/leo-myung/onbid/.env`를 하드코딩 참조하던 것을 `__file__` 기준 상대경로로 수정 (이 PC에서 401 오류 원인)
- **회차 선택 버그 발견**: 동일 물건의 여러 예약 회차 중 `pbctNsq`(회차번호) 최댓값을 "최신 회차"로 골랐는데, 온비드 압류재산은 미래 회차를 미리 예약해두는 구조라 최댓값은 사실 **가장 먼 미래의 최다할인 회차**였음. 존재하지 않는 미래 최저가로 오포롯데캐슬(광주)·건영아파트(구리) ROI를 계산해 잘못된 추천을 함
  - `call_pbanc_cltr_api`, `apply_filters` 두 군데를 `cltrBidBgngDt`(입찰시작일시) 최솟값 기준으로 수정
  - 재발방지: `.claude/agents/data-verifier.md` 신규 에이전트 작성, `onbid-auction-orchestrator` 파이프라인에 Phase 1.5(검색결과 검증)·Phase 3.5(계산 입력값 검증) 추가
  - 전역 메모리 `onbid-round-selection-bug` 기록
- 수정 후 재검색 결과: 대부분 물건이 현재 회차 기준 **할인율 0%**(감정가 그대로)로 확인 — 이전 "40~50% 할인" 추천은 정정 대상
- 실제 즉시 할인 매력 있는 물건은 **시흥시 신천동 37-6 (50% 할인, 6,050만원, 입찰기간 07/27~29)**만 확인됨

## 5. 시흥시 신천동 물건 재개발 사업성 확인
- "가로주택정비사업(사업시행계획인가 전)" 명시된 물건 → 웹검색으로 사업 실체 확인
- 추정: "동경1·2차" 가로주택정비사업(신천동 37-3번지 일원, 3,035㎡, 83→100세대, 최고14층), **2021년 5월 조합설립인가** — 단 37-6번지가 이 구역에 포함되는지는 언론자료로 100% 확정 못함(지번 인접 추정)
- 사업성 평가: 조합설립 후 5년째 사업시행인가 전(가로주택정비 표준기간 2~3년 훌쩍 초과) — 지연 신호. 순증 세대수 17세대뿐이라 일반분양 물량 부족, 추가분담금 우려. 다만 최저가 자체가 워낙 낮아 하방 리스크는 제한적
- 결론: 재개발 프리미엄 베팅 근거는 약함, 저가매수 관점에서만 검토 권장. 시흥시청 도시정비과 확인 필요

## 산출물
- `_workspace/01_search_results.json` (경기도 아파트 검색, 수정본)
- `_workspace/detail_*.json`, `_workspace/오포롯데캐슬_전체회차.json`
- `.claude/skills/onbid-ranking-stats/`, `.claude/agents/data-verifier.md`
- `scripts/fetch_ranking_stats.py`, `scripts/search_properties.py` (수정)
